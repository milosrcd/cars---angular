import { CarBackgroundDirective } from './car-background.directive';

describe('CarBackgroundDirective', () => {
  it('should create an instance', () => {
    const directive = new CarBackgroundDirective();
    expect(directive).toBeTruthy();
  });
});
