import { FullModelNamePipe } from './full-model-name.pipe';

describe('FullModelNamePipe', () => {
  it('create an instance', () => {
    const pipe = new FullModelNamePipe();
    expect(pipe).toBeTruthy();
  });
});
